# The favicons and the social share cards in assets/og are generated but
# deliberately NOT ignored. They are committed, so GitHub Pages serves a
# correct icon and share card the moment the site goes up.

# Local noise
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
